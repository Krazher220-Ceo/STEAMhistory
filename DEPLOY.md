# 🚀 Инструкция по деплою на Vercel

## Способ 1: Через веб-интерфейс Vercel (Рекомендуется)

1. **Создайте аккаунт на Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Зарегистрируйтесь через GitHub, GitLab или Email

2. **Подготовьте проект:**
   - Загрузите проект в GitHub/GitLab репозиторий
   - Или используйте существующий репозиторий

3. **Деплой:**
   - Войдите в Vercel Dashboard
   - Нажмите "Add New Project"
   - Подключите ваш репозиторий
   - Vercel автоматически определит Next.js
   - Нажмите "Deploy"

4. **Готово!**
   - Ваш сайт будет доступен по адресу: `your-project.vercel.app`

## Способ 2: Через Vercel CLI

1. **Установите Vercel CLI:**
```bash
npm i -g vercel
```

2. **Войдите в аккаунт:**
```bash
vercel login
```

3. **Перейдите в папку проекта:**
```bash
cd steam_social_sphere_website
```

4. **Деплой:**
```bash
vercel
```

5. **Для production деплоя:**
```bash
vercel --prod
```

## Способ 3: Через GitHub Actions (Автоматический деплой)

1. Создайте файл `.github/workflows/deploy.yml`:
```yaml
name: Deploy to Vercel
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: amondnet/vercel-action@v20
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
```

2. Добавьте секреты в GitHub:
   - `VERCEL_TOKEN` - токен из Vercel Settings
   - `ORG_ID` - ID организации
   - `PROJECT_ID` - ID проекта

## ⚙️ Настройки проекта

Vercel автоматически определит:
- **Framework:** Next.js
- **Build Command:** `npm run build`
- **Output Directory:** `.next`
- **Install Command:** `npm install`

## 📝 Переменные окружения

Если нужны переменные окружения:
1. В Vercel Dashboard → Project Settings → Environment Variables
2. Добавьте нужные переменные
3. Передеплойте проект

## 🔄 Обновление сайта

После каждого push в main ветку, Vercel автоматически:
- Соберёт проект
- Задеплоит новую версию
- Отправит уведомление

## 📊 Мониторинг

В Vercel Dashboard вы можете:
- Видеть логи сборки
- Мониторить производительность
- Настраивать домены
- Управлять версиями

## 🆘 Решение проблем

**Ошибка сборки:**
- Проверьте логи в Vercel Dashboard
- Убедитесь, что все зависимости в `package.json`
- Проверьте версию Node.js (Vercel использует 18.x)

**Карты не работают:**
- Убедитесь, что Leaflet CSS импортирован
- Проверьте, что компоненты загружаются динамически

**Стили не применяются:**
- Проверьте `tailwind.config.js`
- Убедитесь, что `globals.css` импортирован в `layout.tsx`

---

**Удачи с деплоем!** 🎉

