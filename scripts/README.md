# 🐍 Локальная генерация изображений через Stable Diffusion

## 📋 Установка

### 1. Установите зависимости Python

```bash
cd scripts
pip install -r requirements.txt
```

### 2. Для Apple Silicon (M1/M2/M3)

Если у вас Mac с Apple Silicon, используйте:
```bash
pip install torch torchvision torchaudio
```

### 3. Для NVIDIA GPU

Если у вас NVIDIA GPU, установите CUDA версию PyTorch:
```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

## 🚀 Использование

### Через API (автоматически)

Просто используйте интерфейс в приложении - локальная генерация будет использоваться автоматически, если доступна.

### Вручную через командную строку

```bash
python3 scripts/generate_image.py "Soviet interior 1970s Kazakhstan"
```

## ⚙️ Настройка

### Изменение модели

Отредактируйте `generate_image.py` и измените `model_id`:

```python
model_id = "runwayml/stable-diffusion-v1-5"  # Текущая модель
# Или используйте другую:
# model_id = "CompVis/stable-diffusion-v1-4"
# model_id = "stabilityai/stable-diffusion-2-1"
```

### Оптимизация производительности

- **Для GPU (CUDA):** Автоматически используется float16 и оптимизации
- **Для Apple Silicon (MPS):** Используется float32 (MPS не поддерживает float16)
- **Для CPU:** Используется float32, но будет медленнее

### Уменьшение времени генерации

В файле `generate_image.py` можно изменить:
- `num_inference_steps` - меньше шагов = быстрее, но ниже качество
- `width` и `height` - меньший размер = быстрее

## 📝 Требования

- Python 3.8+
- PyTorch (устанавливается автоматически)
- Минимум 4GB RAM (рекомендуется 8GB+)
- Для GPU: CUDA 11.8+ или Apple Silicon

## 🔧 Устранение проблем

### Ошибка "CUDA out of memory"

Уменьшите размер изображения или используйте CPU:
```python
device = "cpu"
```

### Медленная генерация на CPU

Используйте меньше шагов:
```python
num_inference_steps=15  # Вместо 20
```

### Проблемы с установкой

Попробуйте установить зависимости по отдельности:
```bash
pip install torch
pip install diffusers
pip install pillow
```

---

**Готово! Локальная генерация работает автоматически.** 🎉


